#include "lib.h"
float numeroMassimo( const float numeri[], int dim){
  numeri [dim];
  float massimo = numeri[0];
  fpr (int i=0; i<dim; i++){
    if(massimo<numeri[i];
       }
       }
       return massimo;
       }
    
